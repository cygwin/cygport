SUBDIRS = bin lib data doc tests

EXTRA_DIST = CYGWIN-PATCHES AUTHORS COPYING ChangeLog INSTALL NEWS README TODO

dist-hook:
	chmod 644 $(top_distdir)/INSTALL

DISTCLEANFILES = $(PACKAGE)-$(VERSION).tar.bz2
