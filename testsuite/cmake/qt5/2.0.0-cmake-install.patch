--- origsrc/libqtxdg-2.0.0/qtxdg/CMakeLists.txt	2016-09-17 07:06:08.000000000 -0500
+++ src/libqtxdg-2.0.0/qtxdg/CMakeLists.txt	2016-11-06 18:13:30.992750600 -0600
@@ -122,8 +122,11 @@ foreach(h ${libqtxdg_PRIVATE_INSTALLABLE
 endforeach()
 
 install(TARGETS
-    ${QTXDGX_LIBRARY_NAME} DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    ${QTXDGX_LIBRARY_NAME}
     EXPORT "${QTXDGX_FILE_NAME}-targets"
+    LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    RUNTIME DESTINATION "bin"
+    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     COMPONENT Runtime
 )
 
--- origsrc/libqtxdg-2.0.0/xdgiconloader/CMakeLists.txt	2016-09-17 07:06:08.000000000 -0500
+++ src/libqtxdg-2.0.0/xdgiconloader/CMakeLists.txt	2016-11-06 18:18:45.651369200 -0600
@@ -66,8 +66,11 @@ set_target_properties(${QTXDGX_ICONLOADE
 export(TARGETS ${QTXDGX_ICONLOADER_LIBRARY_NAME} FILE "${CMAKE_BINARY_DIR}/${QTXDGX_ICONLOADER_FILE_NAME}-targets.cmake")
 
 install(TARGETS
-    ${QTXDGX_ICONLOADER_LIBRARY_NAME} DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    ${QTXDGX_ICONLOADER_LIBRARY_NAME}
     EXPORT "${QTXDGX_ICONLOADER_FILE_NAME}-targets"
+    LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    RUNTIME DESTINATION "bin"
+    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     COMPONENT Runtime
 )
 
